# Commit Message Rules（提交信息生成规则）

> 触发条件：仅当用户要求「生成/撰写提交信息」或要求代理执行 git commit 时，适用本规则。

## 工作流程
1. 生成前先读取实际变更：优先 `git diff --staged`（已暂存）；若无暂存内容，再看 `git status` 与 `git diff`，并先与用户确认提交范围。
2. 只基于实际变更撰写，不臆测、不夸大；信息不足（如不清楚改动目的）时先向用户提问。
3. 若一次变更混合多种类型（新增 + 修复 + 重构等），建议拆分为多次提交，并按拆分方案分别输出提交信息。
4. 执行提交时直接使用生成的提交信息，不附加任何署名或 Co-authored-by 行。

## 输出格式
<type>(<scope>): <简要描述>

<详细说明：改了什么、为什么改、关键实现思路>

<涉及文件清单：逐行列出变更文件及其作用>

<可选尾部引用：Closes #123 / Refs #45 / BREAKING CHANGE: 说明>

## type 判定规则（重点区分）
- feat：新增功能、新增用户可感知的能力（新接口、新组件、新交互）
- fix：修复缺陷，修正错误行为（崩溃、计算错误、边界问题）
- refactor：不改变外部行为的重构（调整结构、重命名、抽取函数、消除重复）；不得包含功能新增或缺陷修复
- docs：文档；style：格式/空白（不影响逻辑）；perf：性能优化；test：测试
- build：构建系统或依赖变更；ci：CI 配置；chore：其他杂项维护
- 依赖变更统一使用 build(deps) 或 chore(deps)，描述中写明包名与版本区间（如 `升级 drizzle-orm 0.44 → 0.45`）
- 数据库 schema/迁移变更使用 feat(db) 或 refactor(db)，正文必须注明是否包含 migration 文件及是否需要手动执行
- 若一次变更同时包含多种类型，必须拆分为多条提交信息分别输出，并建议如何拆分提交

## 判定优先级
当难以区分时按此顺序判断：是否修复了错误行为 → fix；是否引入新能力 → feat；仅调整内部实现且行为不变 → refactor。

## 写作要求
1. 描述用中文，简洁明确，首行不超过 72 个字符
2. scope 为受影响的模块或目录名，可省略但建议保留
3. 正文说明「改了什么 + 为什么改」，避免只复述文件名
4. 涉及文件部分列出每个变更文件，并用一句话概括该文件的改动
5. 不夸大、不臆测：只基于实际变更内容撰写，信息不足时先向我提问
6. 禁止模糊表述：不得出现「修改了一些代码」「优化逻辑」「完善功能」「更新文件」等无信息量的描述，必须写清具体改动
7. 有对应的 issue 或任务编号时，在尾部添加 `Closes #编号`（关闭）或 `Refs #编号`（关联）
8. 破坏性变更需在末尾标注 `BREAKING CHANGE: <说明>`

## 输出约束
向用户输出提交信息时，只输出提交信息本身，不要附加任何解释、前言或代码块包裹符号。

## 示例
输入：将订单列表的循环查询改为批量查询，涉及 order.service.ts、order.repo.ts，关联任务 #12
输出：
refactor(order): 订单列表查询改为批量查询

将逐条查询订单详情的循环逻辑重构为一次性批量查询，消除 N+1 问题；
外部接口与返回结果保持不变，仅调整内部实现。

涉及文件：
- src/order/order.service.ts：移除循环调用，改为调用批量查询方法
- src/order/order.repo.ts：新增 findOrdersByIds 批量查询方法

Refs #12
